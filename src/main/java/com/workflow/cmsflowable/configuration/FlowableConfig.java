package com.workflow.cmsflowable.configuration;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FlowableConfig {
    // Manual deployment disabled - use /api/v1/deploy endpoints instead
    // This allows for dynamic deployment and better error handling
}