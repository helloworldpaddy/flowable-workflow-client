<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define the log directory -->
    <property name="LOG_DIR" value="logs"/>
    <property name="LOG_FILE" value="cms-flowable"/>

    <!-- Console Appender (for development) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender with Rolling Policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- Max file size before rolling -->
            <maxFileSize>50MB</maxFileSize>
            <!-- Keep max 30 days of log files -->
            <maxHistory>30</maxHistory>
            <!-- Total log files size cap -->
            <totalSizeCap>1GB</totalSizeCap>
            <!-- Delete old log files when app starts -->
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- Separate Error Log File -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <file>${LOG_DIR}/${LOG_FILE}-error.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Async Appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>1024</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- Application-specific loggers -->
    <logger name="com.workflow.cmsflowable" level="${APP_LOG_LEVEL:-INFO}"/>
    
    <!-- Flowable loggers -->
    <logger name="org.flowable" level="${FLOWABLE_LOG_LEVEL:-INFO}"/>
    <logger name="org.flowable.engine.impl.persistence" level="WARN"/>
    <logger name="org.flowable.dmn.engine.impl.persistence" level="WARN"/>
    
    <!-- Spring loggers -->
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.boot" level="INFO"/>
    
    <!-- Database loggers -->
    <logger name="org.hibernate.SQL" level="${SQL_LOG_LEVEL:-WARN}"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>
    
    <!-- Suppress verbose logging -->
    <logger name="org.apache.http" level="WARN"/>
    <logger name="org.springframework.core.io" level="INFO"/>
    <logger name="org.springframework.context.annotation" level="INFO"/>

    <!-- Profile-specific configurations -->
    <springProfile name="dev,development">
        <!-- Development: Log to both console and file -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        <!-- Enable more verbose logging in development -->
        <logger name="com.workflow.cmsflowable" level="DEBUG"/>
        <logger name="org.flowable.engine.repository" level="DEBUG"/>
    </springProfile>

    <springProfile name="staging,test">
        <!-- Staging/Test: Log to file only with moderate verbosity -->
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
    </springProfile>

    <springProfile name="prod,production">
        <!-- Production: Log to file only, minimal verbosity -->
        <root level="WARN">
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
        <!-- Application logs at INFO level in production -->
        <logger name="com.workflow.cmsflowable" level="INFO"/>
        <logger name="org.flowable" level="WARN"/>
    </springProfile>

    <!-- Default configuration (if no profile is active) -->
    <springProfile name="!dev &amp; !development &amp; !staging &amp; !test &amp; !prod &amp; !production">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ERROR_FILE"/>
        </root>
    </springProfile>
</configuration>