<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="001-initial-schema-baseline" author="cms-system">
        <comment>Baseline changeset - existing schema is already in place</comment>
        
        <!-- This is a baseline changeset for existing tables -->
        <!-- Tables are already created by JPA/Hibernate or manual DDL -->
        <sql>
            SELECT 1; -- No-op SQL to mark this changeset as executed
        </sql>
    </changeSet>

</databaseChangeLog>